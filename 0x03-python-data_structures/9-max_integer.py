#!/usr/bin/python3
def max_integer(my_list=[]):
    if len(my_list) == 0:
        return 'None'
    else:
        for i in range(len(my_list)):
            if my_lits[i] > max:
                max = my_list[i]
            return max
